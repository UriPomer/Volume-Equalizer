# Volume Equalizer 产品行为规范

## 目标与测量口径

扩展控制网页 video/audio 的节目增益，默认目标 -21 LUFS。
扩展开启、成功绑定并且音频处理器正常时，最终输出的每个 400 ms
瞬时响度窗口不得超过目标 +2 LU。瞬时/短时响度使用共享的 K 加权及
BS.1770 声道权重；短时窗口为精确连续 3 秒。未积满窗口显示“测量中”，
静音显示 -∞。LFE 不计入 LUFS，但仍接受峰值保护。

节目积分响度用于正常增益调节，不作为当前有声/静音的判据。
面板显示实际输出测量、节目增益与安全衰减合成后的近似当前倍率，以及独立安全衰减倍率。
由于输出存在缓冲，瞬时倍率读数与输入有约 215 ms 的时间差。

## 音频处理与安全优先级

处理顺序：媒体源 → 节目 gain → 低频增强 → 关联峰值限幅 →
预读响度保护 → 输出。预读响度保护先收集 100 ms，再用 100 ms
分摊窗口计算；加上峰值预读，总音频延迟约 215 ms。
它检查跨块的滑动窗口及滤波器余响，在音频播放前确定仅衰减的安全倍率，
每个 100 ms 块保留后续窗口预算，避免前块用满预算造成掉音；
释放曲线以 250 ms 时间常数恢复，并始终受安全上限约束。
保护在 AudioWorklet 中持续运行，不依赖主线程刷新率
或页面可见性，既适用于实时模式，也适用于完整音轨模式。

普通增益上下限、校准锚点与稳态限速只约束节目调节。安全降压可以突破
普通下限和下降速率；低频增强后的实际音频同样受最终输出保护。
输出保护优先于积分响度精度及最终增益稳定性，动态很大的节目可能低于目标平均值。

## 场景行为

| 场景 | 行为与可观察结果 |
|---|---|
| 首次播放/新媒体源 | 前 10 秒校准节目增益；前 6 秒不主动放大到 1x 以上。当前静音或测量不可用时不升压。首个有效声音也经过输出保护。 |
| 正常持续播放 | 校准后在锚点 ±0.2x 与 ±0.75 dB 交集内缓慢调整，普通变化不超过 0.1x/分钟。安全降压可超出这些范围。 |
| 安静/静音后出现响声 | 历史积分不能打开噪声门；新响声在预读缓冲内接受最终响度保护。 |
| 切入后台 | 冻结节目增益并保留安全衰减；输出保护持续执行。 |
| 恢复前台/同源 seek | 重置 UI 和节目测量代际，保留校准状态；测量重置不清除输出保护的历史预算。 |
| 设置增益上下限 | 不把仍有效的安全衰减抬回普通下限；输出保护始终生效。 |
| 修改目标响度 | 向音频线程发送新目标并重新缓冲。已播放的旧声音不可追溯修改；旧的 400 ms 窗口退出后按新目标验收。 |
| 开启但处理器加载中或失败 | 受控媒体静音，不用普通压缩器冒充 LUFS 保护；面板显示“保护不可用 · 已静音”。 |
| 用户关闭扩展 | 直接旁路到原始音频，不承诺响度上限；不修改媒体元素的 volume/muted 属性。 |
| 重新开启 | 从空节目测量状态重新校准，输出经过保护后播放。 |
| 完整音轨分析成功 | 以完整测量计算固定节目增益；末端安全保护仍持续约束动态片段。 |
| 完整音轨失败/长度不完整 | 显示原因并继续实时模式；同一源不在每次 play 重复下载。未播放媒体不拉取完整音轨。 |
| 绑定被页面 Web Audio 占用 | 显示绑定失败并在后续 play 有限重试；未成功绑定的媒体无法提供保护。 |
| 多声道内容 | 在保护前按浏览器 destination 声道布局混音（默认立体声），保留左右差异；保护后不再发生额外声道复制或降混。完整音轨分析按源布局测量，实际输出按播放布局保护。 |

## 验收与交付

- 全量 TypeScript 检查、单元及控制器集成测试通过。
- 最终 PCM 输出回归覆盖静音后突发声、连续响声、跨块边界、低频增强、
  后台无 UI 回调、测量代际重置、不同采样率与多声道。
- 测试检查实际输出窗口而非仅检查计算出的 gain；包含非零音频断言及缓冲尾部。
- 音频基准按真实因果顺序渲染，与 FFmpeg 离线测量交叉验证。
  完整音轨误差、节目增益稳定性及实际输出上限均纳入门禁。
- 构建后的 Worklet 已打包共享 DSP，不含需要运行时加载源码的引用。
- 包版本、manifest、锁文件一致；本地 ZIP 可解压加载。
