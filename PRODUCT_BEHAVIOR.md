# Volume Equalizer 产品行为规范

## 目标与口径

默认目标为 -21 LUFS。实时模式只读取已播放音频，不获取完整音轨。目标是让不同视频的响段均值接近，同时尽量保持视频内部动态，不是使每一刻声音一样大。

每 100 ms 记录一个连续 400 ms K 加权响度窗口，排除无效值及 ≤-60 LUFS 的近静音。按有效播放时间选择最响 40% 窗口，对线性能量求平均再转回 LUFS。跨窗口有 75% 重叠；这是产品的响段统计，不是 BS.1770 门限积分响度。累计直方图采用 0.1 LU 分桶，边界桶按所需时间比例取能量，空间固定，不依赖视频长度。

目标节目倍率 = 10^((目标 LUFS - 输入响段均值)/20)。输入在低频设置之后、节目增益之前测量；实际输出在峰值削波之后独立测量。面板分别显示响段均值、积分、400 ms 瞬时、调节状态和削波倍率。窗口未满或受非法样本污染显示“测量中”；真实静音显示 -∞，不会以 -∞ 驱动升压。

## 在线增益状态机

1. 收集与校准：新视频从 0.5x 开始（受用户上下限约束）。前 4 秒有效声音不升压，极安静片头也不提供放大依据；当前明显过响可立即向下校准。至少 10 秒有效声音、参考稳定 3 秒且倍率已接近估计值后进入稳定态；累计到 20 秒时可放宽参考稳定时间，但仍需倍率接近估计值。实际经过时间可能更长。
2. 稳定：记住固定锚点，普通调节始终处于宽度至多 0.5x 的固定区间与锚点 -3/+2 dB 区间的交集。靠近用户上下限时向区间内部平移，不随每次微调移动锚点。累计响段估计误差超过 0.1 LU 时收集修正证据；向下需 1 秒、向上需 2 秒代表性声音。降低最多 2 dB/s、0.2x/s；升高最多 0.5 dB/s、0.04x/s，同时受用户速度约束。安静段不产生上调证据，也不执行上调。
3. 响段重校准：当前 3 秒短时响度在原锚点下连续 8 秒高于目标 4 LU，且累计估计要求降低时，每个视频允许一次向下重校准。它用于安静片头后持续更响的主体，不由短爆点触发。面板明确显示，稳定性统计包含重校准，不能靠重新起算隐藏大幅变动。

静音、无效测量、暂停不推进校准也不改变倍率；有效统计保留。稳定性是首次稳定之后的验收，开头收敛过程单独报告。实时算法不能预测未来，短视频、不具代表性的片头和中途内容风格突变会有偏差。

## 音频链路与边界

媒体源 → 低频滤波 → 节目增益 → 约 15 ms 关联峰值削波保护 → 实际输出。低频滤波输出还进入独立输入测量。保护前按浏览器 destination 布局混音，左右声道保持关联比例；LFE 不计入 LUFS，但参与峰值检测。

峰值保护仅防止数字削波，采用带余量的插值峰值估计，不是经过认证的 true-peak 仪表。它不实施目标 +2 LUFS 硬上限。强制瞬时上限与保留原始动态不能同时成立。

用户倍率上下限可能使目标不可达，面板显示“达到倍率限制”。极高峰值素材即使平均不响也可能频繁削波，实际输出偏低、有效倍率超出稳定区间；不能承诺所有未知视频都满足 99%。绝对听感还受视频播放器、系统音量与设备影响。

## 生命周期与设置

| 场景 | 行为 |
|---|---|
| 换源 / B 站视频或分 P 切换 | 清空节目历史和锚点，恢复保守起点；即使复用媒体元素也检查身份。 |
| 后台播放 / 恢复前台 | 音频消息持续推进测量与控制；UI 刷新不驱动 AGC，不因可见性变化清空历史。主线程阻塞会延迟节目控制，但音频线程峰值保护继续。 |
| 同视频 seek | 清空瞬时测量窗口与消息代际，保留累计节目历史和稳定锚点。 |
| 改目标 | 保留输入统计、重新校准；清空旧目标下的输出响段统计。 |
| 改低频设置 | 清空输入/输出节目统计并重新校准，避免混合不同 EQ 的证据。 |
| 改倍率上下限 | 应用新边界，重新允许校准。 |
| 关闭 / 重新开启 | 关闭直接播放原始音频；重新开启从保守起点重新校准。 |
| 处理器加载中 / 失败 | 受控音频静音，显示保护不可用；不释放先前高倍率。 |
| 可选完整音轨模式 | 播放后才拉取音轨；成功后使用独立固定增益目标，失败继续实时。它不实现本文的在线上 40% 目标。 |

## 验收

TypeScript、单元、控制器生命周期测试、浏览器真实 AudioWorklet 渲染及构建检查均应通过。音频基准按先输出当前块、再测量决定下一块的因果顺序运行：

- 实际全程输出响段均值距目标 ≤1.5 LU；包含校准期及输出尾部，不以被压低的参考值替代目标。
- 从首次稳定到视频结束，中央 99% 有效倍率跨度 ≤0.5x，削波帧比例 ≤1%；同时公布完整跨度和重校准次数。
- 官方普通素材之间输出响段均值跨度 ≤3 LU；额外同素材 ±6 dB 变体检查跨视频一致性。
- 输入积分测量与 FFmpeg 参考误差 ≤0.5 LU；这只校验测量，不代替实际输出验收。
- 最终 PCM 测试覆盖单/双/多声道、峰值、非法输入，以及不触发削波时 20 dB 电平差的保留。

本地结果不能代替用户在 B 站长期试听，也不能将有限素材的通过率称为全平台 99% 概率。
