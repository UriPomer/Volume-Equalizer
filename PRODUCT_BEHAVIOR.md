# Volume Equalizer 产品行为规范

## 目标

扩展把网页 `video`/`audio` 的节目响度稳定到用户目标值，默认 `-21 LUFS`，
同时避免页面生命周期、媒体源变化和处理器切换造成突发增益。

## 非目标

- 不改变媒体元素自身的 `volume` 或 `muted` 属性。
- 不保证直播和无法完整拉取的媒体可使用完整音轨分析。
- Worklet 不可用时不使用不连续的快照冒充连续音频测量。

## 行为契约

| 场景与前置条件 | 输入/操作 | 程序动作与状态变化 | 可见结果 | 失败/取消行为 | 验收检查 |
|---|---|---|---|---|---|
| 可见页面正在播放 | 连续音频进入 Worklet | 校验测量代际；校准期仅在短时/瞬时响度高出目标至少 3 LU 时覆盖积分值，前 6 秒最多恢复到 `1.0x`，最后 4 秒才允许放大并建立锚点 | 真正突发响声会及时衰减，接近目标的片段不会错误压低整段锚点 | 无有效测量时保持 gain | 单测和音频基准 |
| 实时模式已观察 10 秒 | 继续播放 | 记录校准锚点，之后 gain 限制在锚点 `±0.2x` 与 `±0.75 dB` 的交集，并继续受全局上下限约束；瞬态峰值只交给末端 limiter | 后续节目 gain 总跨度不超过 1.5 dB，不因单个峰值抽动 | reset/source change 后重新校准 | gain-control 单测 |
| 页面切入后台 | `visibilitychange` | 冻结当前 gain、取消旧测量代际、停止自适应 | 后台不会自动升高音量 | 自动事件只能保持或降低冻结 gain | controller 集成测试 |
| 页面恢复前台 | `visibilitychange` | 新建测量代际，丢弃后台旧消息并重新校准 | 不继承后台测量历史 | 无新测量时保持冻结 gain | controller 集成测试 |
| 用户关闭扩展 | 点击关闭 | 路由到 unity bypass，清空实时测量 | 原始媒体音量 | 保存失败只记录错误，不改变当前页状态 | controller/settings 测试 |
| 用户重新开启扩展 | 点击开启 | gain 从 `1.0x` 和空测量状态开始，随后平滑校准 | 不复用关闭前的危险 gain | Worklet 未就绪时保持安全 gain | controller 集成测试 |
| 媒体源变化 | `emptied`/新 metadata | 取消旧分析、清空结果和测量代际、重新校准 | 旧源状态不污染新源 | 后台仍受冻结上限保护 | controller 集成测试 |
| Worklet 加载或运行失败 | processor error | 停止实时自适应，保留安全 gain，使用末端 limiter | 面板显示“处理器不可用”，音频继续播放但暂不均衡 | 不启用重叠 Analyser 快照 AGC | controller 集成测试 |
| 完整音轨分析失败 | 网络、解码、大小或完整性失败 | 保留实时模式并记录该媒体源已尝试 | 面板显示失败 | 同一媒体源不因每次 play 重复下载；源变化或用户重开模式后可重试 | controller/full-analysis 测试 |
| 设置在其他标签页变化 | `chrome.storage.onChanged` | 规范化后应用到当前控制器 | 已打开页面保持一致 | 非法字段回退或截断到安全范围 | settings 测试 |

## 质量门禁

- TypeScript 全量类型检查通过。
- 单元与控制器集成测试通过。
- 正式音频样本输出在目标 `±1 LU`。
- 第 10 秒后 gain 的 `P95–P5 ≤ 1.5 dB`，最大跨度 `≤ 3 dB`。
- 生产构建成功，manifest 版本与发布 tag 一致。
